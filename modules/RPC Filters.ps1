netsh rpc filter delete filter filterkey=all

Write-Output "Go to https://github.com/jsecurity101/MSRPC-to-ATTACK/tree/main/documents and for every page, type 'netsh' then paste (right-click) the command block in it"

Write-Output "For the ones with 2 command blocks, run the second one"